.DS_Store
.serena/

# The site: Astro's build, and Docusaurus's build plus the doc pages
# `sync-docs.mjs` generates from the Markdown in the repository.
/apps/web/.astro/
/apps/docs/.docusaurus/
/apps/docs/build/
/apps/docs/docs/

# Compiler intermediates. `perry compile` writes its object files into the
# directory it is run from, one per module including every dependency's —
# 263 of them, 36MB, straight into the repository root.
*.o

# TypeScript monorepo
node_modules/
dist/
*.tsbuildinfo
.moon/cache/

# fallow's extraction cache; the committed baselines live in .fallow-baselines/
.fallow/
