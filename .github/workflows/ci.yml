name: ci

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    # macOS gate: unit tests plus macos e2e. Linux e2e is check-linux;
    # Windows e2e is check-windows.
    runs-on: macos-latest
    steps:
      # Full history: the commit-message check below ranges from the pull
      # request's base branch to HEAD. rust-cache keys on the manifests, not
      # on how much history is present, so the Rust steps are unaffected.
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Commits pushed from anywhere but this machine never met the local
      # commit-msg hook, so a pull request's messages are linted here too,
      # before the Rust build — a bad subject fails in seconds, not minutes.
      # Pushes to main skip this: every commit there arrived through a pull
      # request that already passed it.
      - name: Read the Node pin from mise.toml
        if: github.event_name == 'pull_request'
        id: node_pin
        run: |
          version=$(grep '^node = ' mise.toml | cut -d '"' -f2)
          [ -n "$version" ] || { echo "no node pin found in mise.toml" >&2; exit 1; }
          echo "version=$version" >> "$GITHUB_OUTPUT"

      # mise owns the pin but is not installed on GitHub runners, so
      # setup-node runs the version mise.toml names. A number written here
      # instead would be a second pin that drifts the first time one changes.
      - uses: actions/setup-node@v4
        if: github.event_name == 'pull_request'
        with:
          node-version: ${{ steps.node_pin.outputs.version }}

      - name: Commit messages
        if: github.event_name == 'pull_request'
        run: |
          npm ci
          # Merge commits are skipped: git writes their subjects, they are
          # not conventional commits, and nobody may rewrite them — the same
          # call the local commit-msg hook makes. commitlint has no flag for
          # it, so each non-merge commit is checked on its own, and every
          # offender is listed before the step fails rather than stopping at
          # the first.
          base="origin/${{ github.base_ref }}"
          # A range that does not resolve must be an error: rev-list would
          # print nothing, the loop would see no commits, and a broken check
          # would pass itself.
          git rev-parse -q --verify "$base^{commit}" > /dev/null \
              || { echo "base branch $base not found" >&2; exit 1; }
          echo "linting $(git rev-list --count --no-merges "$base..HEAD") commit(s) from $base"
          bad=0
          for sha in $(git rev-list --no-merges --reverse "$base..HEAD"); do
              subject=$(git log -1 --format=%s "$sha")
              # Same passthrough as the local hook: git writes reverts in its
              # own format, not as conventional commits.
              case "$subject" in
                  "Revert "*) continue ;;
              esac
              echo "checking $sha: $subject"
              if ! npx --no-install commitlint --from "$sha^" --to "$sha"; then
                  echo "not a conventional commit: $sha $subject" >&2
                  bad=1
              fi
          done
          exit $bad

      - name: Install toolchain
        run: rustup component add rustfmt clippy

      - uses: Swatinem/rust-cache@v2

      - name: Format
        run: cargo fmt --all -- --check

      - name: Lint
        run: cargo clippy --all-targets --locked -- -D warnings

      # Unit tests and the end-to-end suite in tests/, which drives the real
      # binary against a throwaway root. Both are offline.
      - name: Test
        run: cargo test --all-targets --locked

  # Linux compile, unit tests, and tests/install_linux.rs (cfg linux).
  check-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install toolchain
        run: rustup component add clippy

      - uses: Swatinem/rust-cache@v2

      - name: Lint
        run: cargo clippy --all-targets --locked -- -D warnings

      - name: Test
        run: cargo test --all-targets --locked

  # Windows compile, unit tests, and tests/install_windows.rs (cfg windows).
  check-windows:
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - name: Install toolchain
        run: rustup component add clippy

      - uses: Swatinem/rust-cache@v2

      - name: Lint
        run: cargo clippy --all-targets --locked -- -D warnings

      - name: Test
        run: cargo test --all-targets --locked

      - name: PowerShell syntax
        shell: pwsh
        run: |
          $errors = $null
          $null = [System.Management.Automation.Language.Parser]::ParseFile('install.ps1', [ref]$null, [ref]$errors)
          if ($errors) { $errors | Format-List *; exit 1 }
          ./install.ps1 --help

  # Packaging is exercised on every pull request, so it breaks here rather than
  # halfway through a release, after the merge that started one.
  package:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: windows-latest
            target: x86_64-pc-windows-msvc
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - uses: Swatinem/rust-cache@v2

      - name: Shell syntax
        run: |
          bash -n install.sh
          bash -n scripts/package.sh
          bash -n scripts/release.sh

      - name: Package the host target
        run: scripts/package.sh ${{ matrix.target }} dist

      # Everything install.sh relies on: an aggregate SHA256SUMS it can grep, a
      # checksum that verifies, and a `ketch` binary it can find and run.
      - name: The tarball is what install.sh expects
        run: |
          cd dist
          if command -v shasum >/dev/null 2>&1; then
            shasum -a 256 *.tar.gz > SHA256SUMS
            shasum -a 256 -c SHA256SUMS
            hash() { shasum -a 256 "$1" | awk '{print $1}'; }
          else
            sha256sum *.tar.gz > SHA256SUMS
            sha256sum -c SHA256SUMS
            hash() { sha256sum "$1" | awk '{print $1}'; }
          fi

          tarball=ketch-${{ matrix.target }}.tar.gz
          expected=$(grep "$tarball" SHA256SUMS | awk '{print $1}')
          actual=$(hash "$tarball")
          [ "$expected" = "$actual" ] || { echo "checksum line unusable" >&2; exit 1; }

          mkdir -p unpacked && tar -xzf "$tarball" -C unpacked
          binary=$(find unpacked \( -name ketch -o -name ketch.exe \) -type f | head -1)
          [ -n "$binary" ] || { echo "no ketch binary in the tarball" >&2; exit 1; }
          "$binary" --version

      # The cask is generated too, and the `tap` job checks it with `brew
      # style` — after the release has published, where a rejected cask leaves
      # the tap a version behind and needs a whole new release to fix. The
      # checksums are placeholders; rubocop only reads the shape of the file.
      # `brew style` applies its cask rules by the `Casks/` directory name.
      - name: The generated cask passes brew style
        if: runner.os == 'macOS'
        run: |
          mkdir -p cask/Casks
          # Two different digests: rubocop rejects a cask that gives both
          # architectures the same one, which is a real mistake worth catching
          # and a pointless way to fail this check.
          scripts/cask.sh 0.0.0 "$(printf '%064d' 0)" "$(printf '%064d' 1)" \
            > cask/Casks/ketch.rb
          brew style cask/Casks/ketch.rb
